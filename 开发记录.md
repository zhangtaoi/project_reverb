# 开发记录 · Project Reverb

> 记录核心开发历程与关键技术问答。持续更新。

---

## 一、项目定位与方向决策

**来源：** `todo.md` —— 持续迭代高级混响算法，MVP 优先、模块化复用、调研先行。

### 双 MVP 方向（关键决策）

最初只选 Dattorro 作为 MVP 方向，经讨论修正为**双 MVP 并行**：

| 派别 | 算法 | 特点 | 文件夹 |
|---|---|---|---|
| 经验派 | **Dattorro (1997)** | 手工调优的艺术品，无严格频谱控制理论 | `dattorro_reverb/` |
| 理论派 | **FDN 反馈延迟网络** | 矩阵理论 + 声学原理，可精确控制极点/频谱 | `fdn_reverb/`（待实现） |

**为什么修正：** Dattorro 缺乏严格的频谱控制理论支撑，是手工调优的特例；FDN 有完整数学框架（Stautner & Puckette 1982, Jot & Chaigne 1991）。两者是"经验美"与"理论工程"的互补，应各自独立文件夹，共享 `common/` 公共库。

### 技术选型

- **原型语言：Python**（调试简单，比 C/C++ 快得多；后续可移植 C++）
- **提速：Numba JIT**（`njit` 编译逐样本循环）
- **第三方依赖：** numpy、soundfile、numba、scipy

---

## 二、Dattorro 引擎实现

**两个版本并存：**

| 版本 | 目录 | 拓扑 | 风格 |
|---|---|---|---|
| **论文忠实版** | `dattorro_reverb/` | pre-delay → pre-filter → 4 input diffusion → 2 tank halves(cross-feedback) → 6 tap 输出 | 稠密厚重，论文原味 |
| **紧凑简化版** | `dattorro_comb/` | 8 并联梳状 → 2×(3 串联全通) → 2 梯形全通(LFO) → 阻尼 | 轻量明亮，修改自由 |

两者各有千秋，并不是"正确/错误"的关系，而是不同的设计选择。`dattorro_comb/` 于 2026-08-28 从旧版 `dattorro_reverb/` 中抽出独立成文件夹。

### 延迟线布局

```
buf = [preDelay | inDiff×4 | decayDiff1×2 | preDampDelay×2 | decayDiff2×2 | postDampDelay×2]
```

一共 13 条延迟线，扁平化到一块缓冲，偏移和长度预计算。

### 可调参数（`params.md` 集中管理）

| 参数 | 默认值 | 含义 |
|---|---|---|
| pre_delay | 0.1 | 预延迟（占最大 4800 样本的比例） |
| pre_filter | 0.85 | 输入低通滤波量 |
| input_diffusion1 | 0.75 | 输入扩散增益（前 2 个全通） |
| input_diffusion2 | 0.625 | 输入扩散增益（后 2 个全通） |
| decay | 0.75 | 混响槽反馈增益 |
| decay_diffusion | 0.70 | 衰减扩散 1 增益（调制全通） |
| damping | 0.95 | 槽内阻尼低通系数 |
| (派生) decay_diffusion2 | clamp(decay+0.15, 0.25, 0.50) | 衰减扩散 2 增益，由 decay 自动计算 |

### 性能

| 实现 | 性能 |
|---|---|
| Numba JIT（新版，13 条延迟线、更复杂的 tap 计算） | ~2× 实时（比旧版慢，但更忠实） |
| 旧版 Numba JIT（8 梳状+全通，更简单） | ~4× 实时 |

### 代码组织原则（来自 todo.md）

- 不同算法独立文件夹
- 可复用函数放 `common/`（io、delay、params 解析），不重复编写
- 实现尽量简洁，重可维护性，能几行不写一大段

---

## 三、踩坑与修复记录

### 1. 削波严重
- **现象：** wet 路径 RMS 达 0.40（干声仅 0.22），峰值 3.96
- **根因：** 8 路梳状并联未归一化，全通级联进一步放大
- **修复：** 梳状求和 ×1/8（`comb_gain`），wet 路径按干声 RMS 归一化

### 2. `ModuleNotFoundError: common`
- **现象：** `python demo.py`（在 `dattorro_reverb/` 内）报找不到 `common`
- **根因：** 直接运行脚本时 Python 只把当前目录加入 `sys.path`
- **修复：** `demo.py` 顶部 `sys.path.insert(0, parents[1])`

### 4. 调研发现另一个实现方向 → 抽离为独立版本（2026-08-28）
- **背景：** 对比 el-visio/dattorro-verb（C, 61★）发现旧版拓扑与论文不一致
- **处理：** 不是"废弃旧版"——听感对比各有千秋。新版保留在 `dattorro_reverb/`，旧版抽出为 `dattorro_comb/`，两者作为独立算法并存（符合 todo.md"不同算法新建不同文件夹"的原则）
- **差异：** 旧版是 8 并联梳状 → 全通级联，轻量明亮；新版是 tank 结构 → 6 tap 输出，稠密厚重
- **影响：** 日后可复用 `common/` 公共库，参数体系完全独立

### 5. 48kHz WAV 的 soundfile 兼容性
- **现象：** `One More Light.mp3` 被删除后，根目录有 `One More Light.wav` 但 soundfile 的 mp3 支持在某些 DLL 环境下不稳定
- **修复：** 改用 WAV 输入，`run.bat` 默认指向 WAV 版本

### 6. 与 C 参考实现的对比验证（2026-08-28）
- **参考实现：** [el-visio/dattorro-verb](https://github.com/el-visio/dattorro-verb)（C, 61★）
- **方法：** 编译 C 代码，用同一输入文件（44.1kHz WAV）渲染冲激响应和全曲，与我们的 Python 实现逐样本和统计对比
- **44.1kHz 结果：** RMS 0.154 vs 0.154（几乎一致），相关性 0.87，RMS diff 0.074
- **48kHz 结果：** 相关性降至 0.45（原因见下）

#### 已知差异（如实记录）

| 差异项 | 我们的实现 | C 参考实现 | 影响 |
|---|---|---|---|
| **采样率缩放策略** | 延迟长度按 `sr/44100` 等比缩放，保证物理时间（ms）一致 | 延迟长度固定为 48kHz 样本数，不缩放 | 44.1kHz 下两者接近（缩放因子≈1），相关性 0.87；48kHz 下我们缩放 9% 而 C 不缩放，相关性降至 0.45 |
| **延迟线缓冲实现** | 精确长度 + 取模 wrap | 2^n 大小 + 位掩码 wrap | 数学等效，但浮点状态填充路径不同，导致逐样本不可对齐 |
| **浮点累积** | Python float64 + Numba JIT | C double | 滤波器迭代顺序和中间变量精度在 6-7 位后分叉，经 1100 万样本反馈网络放大 |
| **输出 tap 缩放** | 无额外缩放（已移除原先的 ×1/6） | 无缩放，输出原始和 | 已对齐，峰值量级一致（0.15 量级） |
| **LFO 调制方向** | 已按 C 代码修正：0→+16（延迟增加）→0→-16（延迟减少） | 原版逻辑 | 已对齐 |
| **tap 偏移/符号** | 逐样本验证，与 C 代码的 14 个输出项（7L+7R）完全一致 | 原版 | 已对齐，无通道交叉或符号反相 |

#### 结论

- 核心算法已对齐。44.1kHz 下相关性 0.87，听感差异不大
- 剩余差异主要来自**延迟线缓冲实现方式**和**浮点累积路径**，属于 bit-exact 级别，不可消除，也不影响听感
- 采样率缩放策略上**我们的做法更正确**（延迟线定义的是物理时间，应按采样率换算），C 代码本质上是 48kHz 专用
- 未来如需逐样本对齐，需统一采样率缩放策略和缓冲实现方式

---

## 四、关键问答与设计决策

### Q1：输出是纯混响还是最终成品？
**答：** 最终成品。引擎 `process()` 返回纯 wet（L/R），demo 里再与原始 `data` 按 `mix` 混合成 `out` 落盘。

### Q2：为什么 signal 整体变小？
**根因：** 早期用峰值归一化（`out/peak`），峰值 1.539 时整体压了约 6dB。
**教训：** 峰值归一化会破坏响度比例，后弃用。

### Q3：响度匹配是什么？为什么是 -14？
- 响度匹配 = 把成品 RMS 统一到目标 dBFS，剥掉"输入本身响度"的影响（不是响度压制，过轻的会补足）
- -14 来自流媒体国际标准（Spotify/YouTube/Apple），低于 0 dBFS 削波线，余量大

### Q4：源文件远大于 -14 时，匹配后猛然变小，合理吗？【核心决策】
**不合理。** 原曲是 -12.7 dBFS，`loudn_out=-14` 会把它硬压到 -14，导致 mix=0（纯干声）也变轻 1.3dB。

**最终语义（当前采用）：**
- **默认 `loudn_out=none`：保持源文件响度**，只做峰值削波保护（peak_guard）
- 响度匹配是**用户主动选择**的工具：设 `loudn_out=-14`（或其他值）时才强制统一响度
- 这样 `mix=0` 严格等于原声（透明），`mix=0.5` 也自然保留原曲响度级
- 对应代码：`demo.py::finalize(out, loudn, peak_guard)`

**理由：** "混响比例"与"成品响度"是两个正交维度。前者是用户调 mix 的意图，后者不该被默认强加；响度匹配留给有明确需求的场景（如投稿需统一响度）。

---

## 五、使用方式

### 直接跑（推荐用户用 run.bat）

**最简单的方式：** 双击 `run.bat`，默认渲染 hall 预设。
也可以改 `run.bat` 顶部的 `SRC / DST / MIX / LOUDN / PRESET` 五行的值。

### 命令行

```bash
# 论文忠实版（默认路径从 params.md 读取）
python -m dattorro_reverb.demo

# 指定预设
python -m dattorro_reverb.demo --preset=hall

# 指定文件
python -m dattorro_reverb.demo data/input.wav output/output.wav

# 紧凑简化版
python -m dattorro_comb.demo data/input.wav output/output.wav
```

### 参数管理

参数统一在 `params.md` 集中管理，含：
- **Input/Output 区段**：`src`, `dst`（输入输出路径）
- **Algorithm params 区段**：核心算法参数
- **Render params 区段**：mix、响度、限幅器
- **Preset 区段**：plate / room / hall 三套预设

CLI 参数可覆盖 `params.md` 中的值：
```bash
python -m dattorro_reverb.demo --preset=hall --mix=0.3 --loudn_out=-14
```

## 六、待办 / 下一步

- [ ] FDN 反馈延迟网络引擎（理论派 MVP）
- [ ] 音色打磨：预设参数调优（plate/room/hall 的听感微调）
- [ ] 参数校检：decay 等参数接近 1.0 时稳定性警告
- [ ] 多采样率测试：验证 44.1k/48k/96k 下的缩放一致性
